# 开发环境配置
server:
  port: 8888

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myBlog?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: rootroot

  data:
    redis:
      host: localhost
      port: 6379
      password:

# 开发环境开启SQL日志
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.blog: debug
